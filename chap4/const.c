#include <stdio.h>
#include <limits.h>
int main()
{
    printf("max is %d\n", INT_MAX);
    printf("min is %d\n", INT_MIN);
    printf("min is %d\n", CHAR_MAX);
    printf("min is %d\n", CHAR_MIN);
    printf("min is %d\n", SCHAR_MAX);
    printf("min is %d\n", SCHAR_MIN);
    return 0;
}